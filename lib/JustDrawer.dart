import 'package:flutter/material.dart';

void main(){
  runApp(MaterialApp(home: Hmenww()));
}

class Hmenww extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      drawer: Drawer(

      ),
      appBar: AppBar(

        ),
      );
  }

}